using Dojo;
using Dojo.Starknet;

// Model definition for `depths_of_dread::systems::actions::actions::PlayerCreated` model
public class depths_of_dread_PlayerCreated : ModelInstance
{
    [ModelField("player")]
    public FieldElement player;

    [ModelField("username")]
    public FieldElement username;

    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
    }
}
